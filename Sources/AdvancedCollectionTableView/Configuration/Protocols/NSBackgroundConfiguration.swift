//
//  NSBackgroundConfiguration.swift
//  
//
//  Created by Florian Zand on 04.09.22.
//

/*
import AppKit

/**
 A configuration that describes a specific background appearance.

 This protocol provides a blueprint for a background configuration object, which encompasses default styling and content for a content view. The background configuration encapsulates all of the supported properties and behaviors for background view customization. You use the configuration to create the background view.
 */
public protocol NSBackgroundConfiguration {
    /**
     Creates a new instance of the background view using this configuration.
     */
    func makeBackgroundView() -> NSView & NSBackgroundView
    
    /**
     Generates a configuration for the specified state by applying the configuration’s default values for that state to any properties that you haven’t customized.
     */
    func updated(for state: NSConfigurationState) -> Self
}
*/
